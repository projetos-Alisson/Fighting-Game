// Os recursos de script mudaram para a v2.3.0; veja
// https://help.yoyogames.com/hc/en-us/articles/360005277377 para obter mais informações

//If the player is already in  a "hit" state then we do nothing 
function HitPlayer(){
		if (argument0.state != 10 and argument.state != 11)
		
		//Tell the player to start a hit animation
		argument.hit = true;
}

function UpdateHitReation(){
	
	if (hit){
		
		nextState = argument0;	
	}
}